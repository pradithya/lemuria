<p align="center">
  <img src="assets/lemuria-logo.png" alt="Lemuria Logo">
</p>

# Lemuria

Argo CD Pull Request Automation — inspired by [Atlantis](https://www.runatlantis.io/) for Terraform.

Lemuria enables teams to preview, approve, and sync Argo CD applications through GitHub PR or GitLab MR comments. It acts as a bridge between your VCS provider and Argo CD, providing a GitOps-native workflow where infrastructure changes are reviewed as manifest diffs before deployment.

## Features

- **Multi-VCS Support** — Works with both GitHub (via GitHub App) and GitLab (via personal/group access tokens)
- **PR/MR Comment Interface** — Interact via `lemuria plan`, `lemuria sync`, `lemuria unlock`, `lemuria rollback`
- **Auto-diff** — Automatically generate and post manifest diffs when relevant files change
- **Application Locking** — Redis-based distributed locks prevent concurrent syncs to the same application
- **ApplicationSet Support** — Detect and diff applications generated by ApplicationSets (via `ownerReferences`)
- **Multi-source Support** — Handle applications with multiple repository sources
- **Approval Requirements** — Optionally require PR approval before sync (per-app or global)
- **Auto-merge** — Optionally merge PRs after successful sync
- **Rollback** — Revert to a previous deployment with `lemuria rollback --id <history-id>`
- **Web UI** — View locks, application status, and manage users
- **Multiple Auth Methods** — GitHub OAuth, GitLab OAuth, OIDC, and basic auth with RBAC


## Quick Start

### Prerequisites

- Go 1.25+
- Node.js 20+ (for frontend)
- Redis
- Argo CD instance
- GitHub App credentials and/or GitLab access token

### Installation

#### From Source

```bash
git clone https://github.com/pradithya/lemuria.git
cd lemuria
make build            # Builds frontend + backend → bin/lemuria

# Configure
cp config/lemuria.yaml.example lemuria.yaml
# Edit lemuria.yaml with your settings

# Run
./bin/lemuria -config lemuria.yaml
```

#### Docker

```bash
docker build -t lemuria:latest .
docker run -p 4141:4141 -v $(pwd)/lemuria.yaml:/app/config/lemuria.yaml lemuria:latest
```

#### Helm (Kubernetes)

```bash
helm upgrade --install lemuria oci://ghcr.io/pradithya/charts/lemuria \
  --namespace lemuria \
  --create-namespace \
  -f values.yaml
```

The Helm chart includes an optional Redis dependency (Bitnami). See `charts/lemuria/values.yaml` for all options.

## Configuration

### Server Configuration (`lemuria.yaml`)

```yaml
server:
  port: 4141
  host: "0.0.0.0"
  base_url: "https://lemuria.example.com"
  log_level: "info"       # debug, info, warn, error

github:
  webhook_secret: "${GITHUB_WEBHOOK_SECRET}"
  app_id: 12345
  app_private_key: "/path/to/key.pem"

gitlab:
  url: "https://gitlab.com"              # GitLab instance URL (default: https://gitlab.com)
  token: "${GITLAB_TOKEN}"               # Personal or Group Access Token
  webhook_secret: "${GITLAB_WEBHOOK_SECRET}"

argocd:
  server_url: "https://argocd.example.com"
  token: "${ARGOCD_TOKEN}"
  insecure: false
  diff_mode: "branch"     # "branch" or "live"
  temp_app_timeout: "2m"  # timeout for temporary app manifest rendering

redis:
  address: "localhost:6379"
  password: "${REDIS_PASSWORD}"
  db: 0

defaults:
  autoplan: true
  require_approval: false
  auto_merge: false
  merge_method: "squash"
  delete_source_branch: false
  allowed_repos:
    - "org/repo1"
    - "org/*"

auth:
  enabled: false
  session_secret: "${SESSION_SECRET}"
  session_ttl: "24h"
  cookie_domain: "example.com"
  cookie_secure: true
  default_role: "user"
  github:                  # GitHub OAuth
    client_id: "${GITHUB_OAUTH_CLIENT_ID}"
    client_secret: "${GITHUB_OAUTH_CLIENT_SECRET}"
    allowed_orgs: ["my-org"]
  gitlab:                  # GitLab OAuth
    url: "https://gitlab.com"
    client_id: "${GITLAB_OAUTH_CLIENT_ID}"
    client_secret: "${GITLAB_OAUTH_CLIENT_SECRET}"
    allowed_groups: ["my-group"]
  oidc:                    # Generic OIDC
    name: "My SSO"
    issuer_url: "https://sso.example.com"
    client_id: "${OIDC_CLIENT_ID}"
    client_secret: "${OIDC_CLIENT_SECRET}"
    scopes: ["openid", "profile", "email"]
  basic:                   # Basic auth (dev only)
    users:
      - username: admin
        password: admin
        role: admin
```

Multiple config files can be merged: `./bin/lemuria -config base.yaml -config overrides.yaml`

All config values support `${ENV_VAR}` substitution.

### Repository Configuration (`.lemuria.yaml`)

Place in your repository root to customize behavior per repository:

```yaml
version: 1
autoplan: true
require_approval: true

applications:
  - name: "my-app"
    paths:
      - "apps/my-app/**"
      - "base/**"

  - name: "my-app-*"           # Wildcard for ApplicationSet-generated apps
    paths:
      - "apps/my-app/**"
    applicationset: "my-appset"

sync_requirements:
  - name: "production-*"
    require_approval: true
    allowed_users:
      - "admin"
```

## Commands

All commands are triggered by posting a comment on a GitHub PR or GitLab MR.

### `lemuria plan`

Generate and post a diff of what would change if synced.

```
lemuria plan                  # Plan all affected applications
lemuria plan -a my-app        # Plan specific application
lemuria plan --all            # Plan all applications
```

### `lemuria sync`

Trigger Argo CD sync for planned applications.

```
lemuria sync                  # Sync all planned applications
lemuria sync -a my-app        # Sync specific application
lemuria sync --prune          # Enable resource pruning
lemuria sync --dry-run        # Dry-run only
```

### `lemuria unlock`

Release locks and discard plans.

```
lemuria unlock                # Unlock all
lemuria unlock -a my-app      # Unlock specific app
```

### `lemuria rollback`

Revert to a previous deployment.

```
lemuria rollback -a my-app --id 5   # Rollback to history ID 5
```

### `lemuria help`

Show available commands.

## Workflow

1. **Open PR** — Lemuria automatically runs `plan` on affected applications (if `autoplan` is enabled)
2. **Review** — Check the manifest diff posted as a PR comment
3. **Approve** — Get PR approval (if `require_approval` is set)
4. **Sync** — Comment `lemuria sync` to apply changes
5. **Auto-merge** — PR is optionally auto-merged after successful sync
6. **Close/Merge** — Locks are automatically released on PR close or merge

## Development

### Prerequisites

```bash
make tools            # Install golangci-lint, goimports, helm-unittest
```

### Running Locally

```bash
make run              # Start Redis + backend + frontend dev server
                      # Backend: http://localhost:4141
                      # Frontend: http://localhost:5173
```

Or individually:

```bash
make run-redis        # Start Redis in Docker
make run-backend      # Build and run Go backend (uses config/test.yaml)
make run-frontend     # Start Vite dev server
```

### Building

```bash
make build            # Build frontend + backend → bin/lemuria
make build-go         # Build backend only (skip frontend, faster for dev)
make docker-build     # Build Docker image
```

### Testing

```bash
make test             # Run unit tests with race detection + coverage
```

Unit tests use a table-driven approach and cover command parsing, webhook validation/parsing, config loading, path matching, diff formatting, and model behavior. Run specific package tests with:

```bash
go test -v ./internal/commands/...   # Command parser, executor helpers, sync requirements
go test -v ./internal/webhook/...    # HMAC validation, event parsing
go test -v ./internal/config/...     # Config defaults, repo config, repo allowlisting
go test -v ./pkg/diff/...            # Diff rendering, output formatting
```

#### E2E Tests (requires k3d + Argo CD)

```bash
make e2e-setup        # Create k3d cluster with Argo CD + Redis
make test-e2e         # Run e2e tests (10m timeout)
make e2e-teardown     # Cleanup cluster
```

### Code Quality

```bash
make lint             # Run golangci-lint
make fmt              # Format with gofmt + goimports
make fmt-check        # Check formatting (CI)
```

### Project Structure

```
lemuria/
├── cmd/lemuria/              # Application entry point (CLI + server bootstrap)
├── internal/
│   ├── argocd/               # Argo CD REST API client
│   │   ├── client.go         # HTTP client setup
│   │   ├── applications.go   # Application CRUD
│   │   ├── applicationsets.go # ApplicationSet detection
│   │   ├── manifests.go      # Manifest fetching
│   │   ├── diff.go           # Diff computation
│   │   ├── parser.go         # Manifest JSON parsing
│   │   └── tempapp.go        # Temporary app creation for rendering
│   ├── auth/                 # Authentication & authorization
│   │   ├── auth.go           # Auth interface
│   │   ├── github.go         # GitHub OAuth provider
│   │   ├── gitlab.go         # GitLab OAuth provider
│   │   ├── oidc.go           # OIDC provider
│   │   ├── basic.go          # Basic auth (dev)
│   │   ├── rbac.go           # Role-based access control
│   │   ├── session.go        # Redis-backed session store
│   │   └── middleware.go     # HTTP auth middleware
│   ├── commands/             # PR/MR command handling
│   │   ├── parser.go         # Parse "lemuria <cmd>" from comments
│   │   ├── executor.go       # Command orchestration
│   │   ├── plan.go           # Plan: detect apps, fetch manifests, diff
│   │   ├── sync.go           # Sync: lock, trigger sync, report status
│   │   ├── unlock.go         # Unlock: release locks
│   │   ├── rollback.go       # Rollback: revert deployment
│   │   ├── help.go           # Help: list commands
│   │   ├── appdetect.go      # Application detection from changed files
│   │   └── vcs_iface.go      # VCS client interface (GitHub/GitLab)
│   ├── config/               # Configuration management
│   │   ├── config.go         # Structs, defaults, validation
│   │   └── loader.go         # YAML loading with env var substitution
│   ├── github/               # GitHub API operations
│   │   ├── client.go         # GitHub App authentication (ghinstallation)
│   │   ├── comments.go       # PR comment CRUD
│   │   └── files.go          # Changed files detection
│   ├── gitlab/               # GitLab API operations
│   │   ├── client.go         # GitLab access token authentication
│   │   ├── comments.go       # MR note CRUD
│   │   └── files.go          # Changed files and file content retrieval
│   ├── vcs/                  # VCS-agnostic utilities
│   │   └── helpers.go        # Path matching, file filtering
│   ├── lock/                 # Distributed application locking
│   │   ├── manager.go        # Lock manager interface
│   │   └── redis.go          # Redis implementation (7-day TTL)
│   ├── models/               # Shared data structures
│   │   ├── application.go    # App, manifest, diff models
│   │   ├── event.go          # Webhook event models
│   │   ├── lock.go           # Lock models
│   │   └── user.go           # User/session models
│   ├── server/               # HTTP server
│   │   ├── server.go         # Server init, middleware (Chi router)
│   │   ├── routes.go         # Route definitions + handlers
│   │   └── static.go         # Embedded frontend static files
│   └── webhook/              # Webhook processing (GitHub + GitLab)
│       ├── handler.go        # GitHub webhook handler (async processing)
│       ├── parser.go         # GitHub event parsing (PR, comment, review)
│       ├── validator.go      # HMAC signature validation (GitHub)
│       ├── gitlab_handler.go # GitLab webhook handler (async processing)
│       └── gitlab_parser.go  # GitLab event parsing (MR, note)
├── pkg/diff/                 # Public diff utilities
│   ├── renderer.go           # Markdown diff rendering for PR comments
│   └── formatter.go          # Output formatting helpers
├── web/                      # React + TypeScript frontend (Vite)
│   └── src/
│       ├── components/       # Auth, Layout, Locks, Admin components
│       ├── hooks/            # useAuth, useLocks
│       ├── pages/            # Dashboard, LocksPage, AdminPage
│       ├── services/         # API client
│       └── types/            # TypeScript type definitions
├── charts/lemuria/           # Helm chart for Kubernetes deployment
├── e2e/                      # End-to-end tests (k3d + Argo CD)
├── config/                   # Example configuration files
├── docs/                     # Jekyll documentation site
├── static/                   # Embedded frontend build output
├── Makefile                  # Build automation
├── Dockerfile                # Multi-stage Docker build
└── .github/workflows/ci.yaml # CI/CD pipeline
```

## API Endpoints

### Public Endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/webhook/github` | POST | GitHub webhook receiver (HMAC-validated) |
| `/webhook/gitlab` | POST | GitLab webhook receiver (token-validated) |
| `/health` | GET | Health check (always returns 200) |
| `/healthz` | GET | Health check (alias) |
| `/ready` | GET | Readiness check (verifies Redis connectivity) |

### Auth Endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/auth/providers` | GET | List available auth providers |
| `/auth/github/login` | GET | Initiate GitHub OAuth flow |
| `/auth/github/callback` | GET | GitHub OAuth callback |
| `/auth/gitlab/login` | GET | Initiate GitLab OAuth flow |
| `/auth/gitlab/callback` | GET | GitLab OAuth callback |
| `/auth/oidc/login` | GET | Initiate OIDC flow |
| `/auth/oidc/callback` | GET | OIDC callback |
| `/auth/basic/login` | POST | Basic auth login |
| `/auth/logout` | POST | Logout (requires auth) |
| `/auth/me` | GET | Current user info (requires auth) |

### API Endpoints (require auth if enabled)

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/v1/status` | GET | Server status |
| `/api/v1/locks` | GET | List all locks |
| `/api/v1/locks/{app}` | DELETE | Force unlock app (admin only) |
| `/api/v1/users` | GET | List users (admin only) |
| `/api/v1/users/{id}/role` | PUT | Update user role (admin only) |

## License

MIT License - see LICENSE file for details.
