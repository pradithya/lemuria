apiVersion: v1
kind: Service
metadata:
  name: {{ include "lemuria.fullname" . }}
  labels:
    {{- include "lemuria.labels" . | nindent 4 }}
    app.kubernetes.io/component: service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lemuria.selectorLabels" . | nindent 4 }}
