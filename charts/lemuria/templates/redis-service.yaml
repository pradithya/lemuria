{{- if .Values.redis.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "lemuria.fullname" . }}-redis
  labels:
    {{- include "lemuria.redis.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "lemuria.redis.selectorLabels" . | nindent 4 }}
{{- end }}
